import { Box } from "@mui/material";
import { Item } from "../../components/item";

const Home = () => {
  return (
    // 60px is size of navbar
    <Box marginTop="60px">
      <Item />
    </Box>
  );
};

export default Home;
